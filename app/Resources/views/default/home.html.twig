{% extends 'base.html.twig' %}

{% block body %}
    <div class="row text-center justify-content-center align-items-center py-5">
        {{ render(controller('AppBundle:Default:lastUsers')) }}
        <div class="col-7">
            <div class="col-12 py-5">
                <h1 class="pb-4">
                    You wanna play too ?
                    <br>
                    It'll be fun.
                </h1>
            </div>
        </div>
        <div class="w-100"></div>
        <div class="col-2">
            <img class="img-fluid d-block mx-auto py-4" src="{{ asset('assets/images/hangman.png') }}" alt="" />
        </div>
        <div class="w-100"></div>
        <div class="col-auto py-5">
            <a class="btn btn-secondary btn-lg font-weight-bold" href="{{ path('game_homepage') }}">{{ 'Do I actually have a choice ?'|upper }}</a>
        </div>
        <div class="w-100 p-2"></div>
        <div class="col-auto py-5">
            <div class="chat-container">
                <div class="chat-bubble py-4">
                    <a class="h5 text-dark font-weight-bold" href="{{ path('contact') }}">Well... <br>You can send me a message... <br> if you want !</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
